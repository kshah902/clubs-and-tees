# MongoDB Atlas
DATABASE_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/clubs-and-tees?retryWrites=true&w=majority

# Payload CMS
PAYLOAD_SECRET=your-secret-key-at-least-32-characters

# Site URL (used for SEO, sitemaps, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://clubsandtees.com

# Vercel Blob Storage (for media uploads in production)
BLOB_READ_WRITE_TOKEN=
